# Server Files Directory

Place your server JSON files here or set SERVER_FILES_HOST_DIR environment variable to point to your actual server files directory.

Example server file format (xxx.txt):
```json
{
  "ips": ["192.168.1.100"],
  "password": "your_password",
  "os_name": "CentOS"
}
```
